groups:

  - name: "permission.certificate.post.group"
    def:
      - name: "permission.certificate.post.any"
        namespace: "certificate.post"
        action: "*"
        internal: true
      - name: "permission.permission.read"
        namespace: "certificate.post"
        action: "read"
      - name: "permission.permission.write"
        namespace: "certificate.post"
        action: "write"
