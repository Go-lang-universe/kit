groups:

  - name: "permission.reports.post.group"
    def:
      - name: "permission.reports.post.any"
        namespace: "reports.post"
        action: "*"
        internal: true
      - name: "permission.permission.read"
        namespace: "reports.post"
        action: "read"
      - name: "permission.permission.write"
        namespace: "reports.post"
        action: "write"
