groups:

  - name: "permission.staff.post.group"
    def:
      - name: "permission.staff.post.any"
        namespace: "staff.post"
        action: "*"
        internal: true
      - name: "permission.permission.read"
        namespace: "staff.post"
        action: "read"
      - name: "permission.permission.write"
        namespace: "staff.post"
        action: "write"
